# 生产环境配置
# ===================================

# 继承基础配置
source "$(dirname "${BASH_SOURCE[0]}")/base.conf"

# 生产环境特定配置
API_BASE_URL="https://api.example.com"
LOG_LEVEL="ERROR"
VERBOSE=false

# 生产环境不应包含明文密码
# 请使用环境变量: PROD_USERNAME, PROD_PASSWORD